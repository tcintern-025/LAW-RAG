fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
streamlit==1.41.1

langchain==0.3.14
langchain-community==0.3.14
langchain-text-splitters==0.3.5
langchain-chroma==0.2.0
langchain-huggingface==0.1.2
langchain-groq==0.2.3
langgraph==0.2.62
# >=3.0.1 fixes CVE-2025-67644 (SQL injection in SqliteSaver.list()'s
# metadata filter). Checkpoint libraries version independently from
# langgraph itself but keep the same SqliteSaver(conn) constructor and
# `checkpoints`/`writes` table names this project relies on -- if `pip
# install` resolves something that breaks, pin the exact working version
# here the same way requirements.txt already documents for langgraph itself.
langgraph-checkpoint-sqlite==2.0.11


chromadb==0.5.23
sentence-transformers==3.3.1

pypdf==5.1.0
docx2txt==0.8